# 🌍 Travel Planner (React + Vite)

Simple travel planning web app that lets you add trips and export the plan as JSON.  
Built with **React** and **Vite** — lightweight, fast, and perfect for small single-page utilities.

> Short & sweet: add destinations, dates, notes, and download your plan as `travel-plan.json`.

---

## 🔧 Features

- Add / edit / remove trips (destination, dates, notes).  
- Export the entire trip plan as a JSON file (`travel-plan.json`) via the download button.  
- Minimal, mobile-friendly UI built with plain CSS and React.  
- Fast dev-server powered by Vite.

---

🛠️ Tech Stack

Frontend: React, Vite

Languages: JavaScript, HTML, CSS

---

Install & Run Locally

Clone the repo:

git clone https://github.com/Sharma0813/travel-planner.git
cd travel-planner

Install dependencies:

npm install

Start development server:

npm run dev

---

💡 Usage

Click Add Trip to add a new travel entry (destination, start/end date, notes).

Use the Download / Export button to save the current plan as travel-plan.json.

Re-open or import that JSON into future versions (future enhancement).


